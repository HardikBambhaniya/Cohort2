//Array manipulation fns
//push(), pop(), shift(), unshift(), splice(), slice(), concat(), forEach(), map(), filter(), reduce(), find(), sort()

//push() - add element in the end of array
/*function pushElement (arr, element) {
    console.log("orignal array: ", arr);

    arr.push(element);
    console.log("After push the element: ", arr);
}

pushElement([1, 2, 3], 4);  // [1, 2, 3, 4] */





//pop() - remove last element from the array
/*function popElement (arr) {
    console.log("orignal array: ", arr);
    arr.pop();
    console.log("After pop the element: ", arr);
}

popElement([1, 2, 3]);  // [1, 2] */





//shift() - remove first element from the array
/* function shiftElement (arr) {
    console.log("origanl array: ", arr);
    arr.shift();
    console.log("After shift the element: ", arr);
}

shiftElement([1, 2, 3]);  // [2, 3]  */





//unshift() - add element in the front of the array
/* function unshiftElement (arr, element) {
    console.log("orignal array: ", arr);
    arr.unshift(element);
    console.log("After unshift: ", arr);
}

unshiftElement([1, 2, 3], 0);  //[0, 1, 2, 3]  */





//conCat() - Merge two arrays 
/* function conCatElement(firstArr, SecArray) {
    console.log("First Array: ", firstArr);
    console.log("Second array: ", SecArray);

    const finalArr = firstArr.concat(SecArray);
    console.log("Array after concat: ", finalArr);
}

conCatElement([1, 2], [3, 4]); // [1, 2, 3, 4]  */





//forEach() - itrate over on array 
/* const num = [1, 2, 3];

function logThing(str) {
    console.log(str);
}    
num.forEach(logThing);   //output will be:
//1
//2
//3  */





//map() - 

 